# Root Lombok configuration for the whole reactor.
config.stopBubbling = true

# Adds @lombok.Generated to every generated member (getters, setters, builders,
# equals/hashCode/toString). JaCoCo honours that annotation and leaves them out of
# the coverage metric, so the percentage reflects hand-written code only.
lombok.addLombokGeneratedAnnotation = true
